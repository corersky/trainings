

allprojects {
    group = 'com.mapr.training'
    apply plugin: 'idea'
}

subprojects {
    apply plugin: 'java'
    apply plugin: 'eclipse'
}

idea {
    module {
        downloadSources = true
    }
}